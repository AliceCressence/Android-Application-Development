package com.joelfah.gradecalculator.ui.theme

import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp

// Backgrounds
val Background = Color(0xFFFFFFFF)
val Surface = Color(0xFFF7F7F7)
val SurfaceDim = Color(0xFFEEEEEE)

// Borders
val Border = Color(0xFFE5E5E5)
val BorderDash = Color(0xFFCCCCCC)

// Text
val TextPrimary = Color(0xFF0D0D0D)
val TextSecondary = Color(0xFF6B6B6B)
val TextHint = Color(0xFFAAAAAA)

// Interactive
val Accent = Color(0xFF0D0D0D)
val AccentFg = Color(0xFFFFFFFF)

// Semantic
val Success = Color(0xFF2D6A4F)
val SuccessBg = Color(0xFFD8F3DC)
val Danger = Color(0xFF9B2226)
val DangerBg = Color(0xFFFFE8E8)

// Grade badge
val BadgeBg = Color(0xFFF0F0F0)
val BadgeFg = Color(0xFF0D0D0D)

object AppSpacing {
    val xs = 4.dp
    val sm = 8.dp
    val md = 16.dp
    val lg = 24.dp
    val xl = 32.dp
    val xxl = 48.dp
}

object AppRadius {
    val card = 12.dp
    val button = 8.dp
    val badge = 6.dp
}
